#include<iostream>
#include<string>
using namespace std;


int main()  {
    string A, B;
    cin>>A>>B;
    int len1 = A.size();
    int len2 = B.size();
    cout << len1 << " " << len2 << endl;
    cout << A+B << endl;
    swap(A[0], B[0]);
    cout<<A<<" "<<B<<endl;
    return 0;
}
